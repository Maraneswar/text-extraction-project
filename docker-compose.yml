version: '3.8'

services:
  alldocex:
    build: .
    container_name: alldocex-app
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - PORT=8000
    restart: always
