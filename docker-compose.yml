version: '3.8'

services:
  docintel:
    build: .
    container_name: docintel-app
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - PORT=8000
    restart: always
